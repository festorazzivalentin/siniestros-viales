{% extends 'base.html.twig' %}

{% block title %}Nuevo siniestro{% endblock %}

{% block body %}
    <h1>Registrar nuevo siniestro</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_siniestro') }}">Volver al listado de siniestros</a>
{% endblock %}