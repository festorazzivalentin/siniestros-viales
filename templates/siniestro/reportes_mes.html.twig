{# {% extends 'base.html.twig' %}

{% block title %}Reporte Siniestro{% endblock %}
{% block body %}
    <h1>Reporte de siniestros por mes y año</h1>
    {{ render_chart(chart) }}
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Reporte de Siniestros{% endblock %}

{% block body %}
<div class="container py-5">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary mb-0">
            <i class="bi bi-bar-chart-line-fill me-2"></i> Reporte de siniestros
        </h1>
        <div>
            <a href="{{ path('siniestro_reportes_anio') }}" class="btn btn-outline-secondary me-2">
                    <i class="bi bi-graph-up-arrow"></i> Ver por Año
            </a>
            <a href="{{ path('siniestro_index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left-circle"></i> Volver
            </a>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h5 class="card-title mb-3">
                <i class="bi bi-calendar3 me-2"></i>
                Distribución por mes (Año actual)
            </h5>

            <div class="border rounded p-3 bg-light">
                {{ render_chart(chart) }}
            </div>

            <div class="mt-4 text-muted small text-end">
                <i class="bi bi-info-circle"></i>
                Datos simulados para prueba visual
            </div>
        </div>
    </div>

</div>
{% endblock %}