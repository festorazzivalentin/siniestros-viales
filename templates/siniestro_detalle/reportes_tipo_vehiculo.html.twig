{% extends 'base.html.twig' %}

{% block title %}Reporte por Tipo de Vehículo{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-sm border-0 rounded-4">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center rounded-top-4">
            <h4 class="mb-0">
                <i class="bi bi-truck"></i> Distribución por Tipo de Vehículo
            </h4>
            <a href="{{ path('siniestro_reportes_mes') }}" class="btn btn-light btn-sm">
                <i class="bi bi-arrow-left"></i> Volver
            </a>
        </div>

        <div class="card-body">
            <p class="text-muted">
                Este gráfico muestra la cantidad de vehículos involucrados en siniestros viales, categorizados por tipo de vehículo.
            </p>

            <div class="d-flex justify-content-center">
                <div class="chart-container" style="position: relative; height: auto; width: 90%;">
                    {{ render_chart(chart) }}
                </div>
            </div>
        </div>

        <div class="card-footer bg-light d-flex justify-content-end">
            <a href="{{ path('siniestro_detalle_reportes_victima_autor') }}" class="btn btn-outline-primary me-2">
                <i class="bi bi-people-fill"></i> Ver por Víctimas y Autores
            </a>
            <a href="{{ path('siniestro_reportes_anio') }}" class="btn btn-outline-success me-2">
                <i class="bi bi-calendar3"></i> Ver por Año
            </a>
            <a href="{{ path('siniestro_reportes_mes') }}" class="btn btn-outline-info">
                <i class="bi bi-calendar-month"></i> Ver por Mes
            </a>
        </div>
    </div>
</div>
{% endblock %}
