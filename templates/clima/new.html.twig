{% extends 'base.html.twig' %}

{% block title %}Clima nuevo{% endblock %}

{% block body %}
    <h1>Registrar nuevo clima</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_clima') }}">Volver al listado de climas</a>
{% endblock %}