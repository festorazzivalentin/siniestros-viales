{% extends 'base.html.twig' %}

{% block title %}Persona nueva{% endblock %}

{% block body %}
    <h1>Registrar nueva persona</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_persona') }}">Volver al listado de personas</a>
{% endblock %}